name: patch-market-freshness-resilience
on:
  push:
    branches: [fix/runtime-stall-hardening-20260905]
    paths:
      - 'scripts/patch_market_freshness_resilience.py'
      - '.github/workflows/patch-market-freshness-resilience.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/runtime-stall-hardening-20260905
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply market freshness resilience
        run: python scripts/patch_market_freshness_resilience.py
      - name: Remove temporary patch machinery and commit
        run: |
          rm scripts/patch_market_freshness_resilience.py
          rm .github/workflows/patch-market-freshness-resilience.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Harden market freshness and blocking I/O'
          git push origin HEAD:fix/runtime-stall-hardening-20260905
